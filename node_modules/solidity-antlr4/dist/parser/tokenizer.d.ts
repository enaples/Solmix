import { Position } from '../ast/base';
import { SolidityLexer } from '../antlr4';
export type SyntaxTokenType = (typeof SolidityLexer.symbolicNames)[number];
export interface SyntaxToken {
    text: string;
    type: SyntaxTokenType;
    range: [number, number];
    position: Position;
}
export interface TokenizerOptions {
    tolerant?: boolean;
}
export declare const defaultTokenizerOption: TokenizerOptions;
export declare const tokenizer: (source: string, _options?: TokenizerOptions) => SyntaxToken[];
