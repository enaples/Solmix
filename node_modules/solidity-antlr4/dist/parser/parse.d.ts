import { SourceUnit, SyntaxNode } from '../ast';
import { SolidityParser, ParseTree } from '../antlr4';
export interface ParseOptions {
    tolerant?: boolean;
    selector?: (parser: SolidityParser) => ParseTree;
}
export declare const defaultParseOption: ParseOptions;
export declare const parse: <T extends SyntaxNode = SourceUnit>(source: string, _options?: ParseOptions) => T;
