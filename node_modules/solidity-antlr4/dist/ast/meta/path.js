import { BaseNodeString, formatString } from "../base.js";
export class Path extends BaseNodeString {
  type = "Path";
  constructor(ctx, visitor) {
    super(ctx, visitor);
    this.name = formatString(ctx.NonEmptyStringLiteral().getText());
  }
}
