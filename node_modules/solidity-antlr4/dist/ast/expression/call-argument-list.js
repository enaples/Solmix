import { BaseNodeList } from "../base.js";
export const CallArgumentList = class extends BaseNodeList {
  type = "CallArgumentList";
  constructor(ctx, visitor) {
    const args = ctx.namedArgument().length ? ctx.namedArgument() : ctx.expression();
    super(args, visitor);
  }
};
