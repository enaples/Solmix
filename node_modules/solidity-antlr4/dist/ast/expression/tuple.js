import { BaseNodeUnion } from "../base.js";
export const Tuple = class extends BaseNodeUnion {
  // type = 'Tuple' as const;
  constructor(ctx, visitor) {
    super(ctx, [ctx.tupleExpression()], visitor);
  }
};
