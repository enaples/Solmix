import { BaseNodeList, formatString } from "../base.js";
export const AssemblyFlags = class extends BaseNodeList {
  type = "AssemblyFlags";
  constructor(ctx, _visitor) {
    super([], _visitor);
    return ctx.AssemblyFlagString().map((x) => formatString(x.getText()));
  }
};
